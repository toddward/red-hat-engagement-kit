# Cross-Engagement Memory — Index

> Tier 2 institutional memory for this fork. Customer-**agnostic**, sensitivity-
> scrubbed learnings distilled from real engagements via `/memory promote`. These
> persist across every engagement and make the fork smarter over time.
>
> This file is the recall index — **one line per active record** in `records/`:
>
> ```
> - [<type>] <slug> — <one-line hook> (<date>)
> ```
>
> `type` for Tier 2 is usually `pattern` or `insight`. See `POLICY.md` for the
> full schema, the promotion rules, and the mandatory sensitivity scrub that
> guarantees nothing customer-identifying ever lands here.

<!-- Promoted records are indexed below this line -->
