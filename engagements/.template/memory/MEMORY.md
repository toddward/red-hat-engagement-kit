# Engagement Memory — Index

> Tier 1 distilled recall layer for this engagement. `CONTEXT.md` (sibling) is
> the append-only audit trail; this `memory/` tree is the lean view skills recall
> from first.
>
> One line per **active** record in `records/`:
>
> ```
> - [<type>] <slug> — <one-line hook> (<date>)
> ```
>
> Record types: `decision`, `commitment`, `constraint`, `fact`, `score`,
> `stakeholder`, `risk`, `summary`. See `memory/POLICY.md` (repo root) for the
> schema, the remember-vs-summarize knobs, and the Memory Protocol skills follow.

<!-- Records are indexed below this line -->
